// Route for every "New session" affordance (header +, sidebar button, command
// palette, empty states): the joy create page.
export function useNewSessionRoute(): '/joy/new' {
    return '/joy/new';
}

{
  "packageManager": "pnpm@10.34.5",
  "name": "@fny/joy-relay",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13",
    "npm": ">=11.10.0"
  },
  "description": "joy relay — the one server: accounts, pairing, machines, push, durable queue, turns, cancellation, leases, events, attachments, tunnel",
  "scripts": {
    "start": "node server.mjs",
    "test": "vitest run",
    "sim": "node test/sim/run.mjs"
  },
  "dependencies": {
    "@electric-sql/pglite": "0.3.15"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
